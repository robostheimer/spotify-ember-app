<h2>Artists</h2>
<ul class="cards">
  {{#each model as |artist|}}
    {{artist-block
      name=artist.name
      genres=artist.genres
      id=artist.id
      popularity=artist.popularity
      image=artist.image
    }}
  {{/each}}
</ul>
